services:
  render:
    build: .
    ports: ["8000:8000"]
    environment:
      - LUXE_PROVIDER=${LUXE_PROVIDER:-mock}
      - OPENAI_API_KEY=${OPENAI_API_KEY:-}
    volumes:
      - ./renders:/srv/renders
