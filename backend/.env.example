# Copy to .env and edit. All vars are optional; defaults run the MOCK provider.
# NOTE: you do not need to set LUXE_PROVIDER=openai by hand. If OPENAI_API_KEY
# is present in the environment and LUXE_PROVIDER is left unset, the API
# automatically uses the real OpenAI provider (see resolve_provider() in
# app/config.py). Set LUXE_PROVIDER explicitly only to force a specific
# provider regardless of whether a key is present.
LUXE_PROVIDER=mock            # mock | openai | replicate
# OPENAI_API_KEY=sk-...        # set this (name only shown here - never the real value)
LUXE_OPENAI_MODEL=gpt-image-1
LUXE_VISION_MODEL=gpt-4o-mini
LUXE_IMAGE_SIZE=1536x1024
LUXE_IMAGE_QUALITY=high
LUXE_CONTROL_MODE=depth        # depth | canny | none
LUXE_OUT_DIR=./renders
LUXE_PUBLIC_BASE=/renders
LUXE_CORS_ORIGINS=*
# Phase B:
# REPLICATE_API_TOKEN=r8_...
# LUXE_REPLICATE_MODEL=black-forest-labs/flux-...
# API integration
LUXE_API_KEY=change-me-in-production
LUXE_JOB_DB=./luxe_jobs.sqlite3
LUXE_JOB_WORKERS=1
LUXE_VIEWER_BASE=/viewer
